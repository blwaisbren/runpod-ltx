# Keep the build context tiny — only the Dockerfile + scripts are needed.
.git
.github
*.md
docs
runpod
# Bundle only the AnimateDiff example workflow into the image; ignore the rest of workflows/.
workflows/*
!workflows/animatediff
models
output
input
.env
.DS_Store
